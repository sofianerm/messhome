modules = ["nodejs-20"]
run = "cd apps/web && npm install && npm run build && npm start"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd apps/web && npm install && npm run build && npm start"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 4000
externalPort = 80
